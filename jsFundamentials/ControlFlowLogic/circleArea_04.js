/**
 * Created by k.asenov on 21.2.2017 ã..
 */
function calculateCircleArea(radius){
    const PI = Math.PI;
    let area= Math.pow(radius*1,2)*PI;
    console.log(area);
    console.log(area.toFixed(2));

}

calculateCircleArea(5);